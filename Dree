-- DEX HUB - UI FUNCIONAL
local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")
local player = Players.LocalPlayer

------------------------------------------------
-- FUNÇÃO DE ARRASTAR (GENÉRICA)
------------------------------------------------
local function makeDraggable(frame)
	local dragging = false
	local dragStart, startPos

	frame.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1
		or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = frame.Position
		end
	end)

	frame.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1
		or input.UserInputType == Enum.UserInputType.Touch then
			dragging = false
		end
	end)

	UIS.InputChanged:Connect(function(input)
		if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement
		or input.UserInputType == Enum.UserInputType.Touch) then
			local delta = input.Position - dragStart
			frame.Position = UDim2.new(
				startPos.X.Scale,
				startPos.X.Offset + delta.X,
				startPos.Y.Scale,
				startPos.Y.Offset + delta.Y
			)
		end
	end)
end

------------------------------------------------
-- GUI
------------------------------------------------
local gui = Instance.new("ScreenGui")
gui.Name = "DexHubGUI"
gui.ResetOnSpawn = false
gui.Parent = player:WaitForChild("PlayerGui")

------------------------------------------------
-- ÍCONE
------------------------------------------------
local icon = Instance.new("TextButton", gui)
icon.Size = UDim2.new(0,50,0,50)
icon.Position = UDim2.new(0,20,0.5,-25)
icon.Text = "DEX"
icon.BackgroundColor3 = Color3.fromRGB(255, 255, 0)
icon.TextScaled = true
icon.BorderSizePixel = 0

makeDraggable(icon)

------------------------------------------------
-- HUB
------------------------------------------------
local hub = Instance.new("Frame", gui)
hub.Size = UDim2.new(0,380,0,230)
hub.Position = UDim2.new(0.5,-190,0.5,-115)
hub.BackgroundColor3 = Color3.fromRGB(255,255,0)
hub.Visible = false
hub.BorderSizePixel = 0

makeDraggable(hub)

------------------------------------------------
-- ABRIR / FECHAR
------------------------------------------------
icon.MouseButton1Click:Connect(function()
	hub.Visible = not hub.Visible
end)

------------------------------------------------
-- TÍTULO
------------------------------------------------
local title = Instance.new("TextLabel", hub)
title.Size = UDim2.new(1,0,0,30)
title.Text = "DEX HUB"
title.BackgroundTransparency = 1
title.TextScaled = true
title.Font = Enum.Font.SourceSansBold

------------------------------------------------
-- SIDEBAR
------------------------------------------------
local sidebar = Instance.new("Frame", hub)
sidebar.Position = UDim2.new(0,0,0,30)
sidebar.Size = UDim2.new(0,100,1,-30)
sidebar.BackgroundColor3 = Color3.fromRGB(240,240,0)

------------------------------------------------
-- MAIN
------------------------------------------------
local main = Instance.new("Frame", hub)
main.Position = UDim2.new(0,110,0,40)
main.Size = UDim2.new(1,-120,1,-50)
main.BackgroundColor3 = Color3.fromRGB(255,255,0)
main.BorderSizePixel = 0

local function clearMain()
	for _,v in pairs(main:GetChildren()) do
		v:Destroy()
	end
end

------------------------------------------------
-- SWITCH
------------------------------------------------
local function createSwitch(text, y, callback)
	local label = Instance.new("TextLabel", main)
	label.Text = text
	label.Position = UDim2.new(0,10,0,y)
	label.Size = UDim2.new(0,120,0,30)
	label.BackgroundTransparency = 1
	label.TextScaled = true

	local btn = Instance.new("TextButton", main)
	btn.Position = UDim2.new(0,140,0,y)
	btn.Size = UDim2.new(0,60,0,30)
	btn.Text = "OFF"
	btn.BackgroundColor3 = Color3.fromRGB(255,0,0)
	btn.TextScaled = true

	local state = false
	btn.MouseButton1Click:Connect(function()
		state = not state
		btn.Text = state and "ON" or "OFF"
		btn.BackgroundColor3 = state and Color3.fromRGB(0,255,0) or Color3.fromRGB(255,0,0)
		callback(state)
	end)
end

------------------------------------------------
-- ESP MENU
------------------------------------------------
local function espMenu()
	clearMain()

	createSwitch("ESP Vermelho", 10, function(on)
		print("ESP Vermelho:", on)
	end)

	createSwitch("ESP Verde", 50, function(on)
		print("ESP Verde:", on)
	end)
end

------------------------------------------------
-- FUNÇÕES MENU
------------------------------------------------
local function funcMenu()
	clearMain()

	createSwitch("Voar", 10, function(on)
		print("Voar:", on)
	end)

	createSwitch("Teleporte", 50, function(on)
		print("Teleporte:", on)
	end)
end

------------------------------------------------
-- INFO MENU
------------------------------------------------
local function infoMenu()
	clearMain()

	local info = Instance.new("TextLabel", main)
	info.Size = UDim2.new(1,-10,1,-10)
	info.Position = UDim2.new(0,5,0,5)
	info.TextWrapped = true
	info.TextYAlignment = Enum.TextYAlignment.Top
	info.BackgroundTransparency = 1
	info.TextScaled = true
	info.Text = "DEX HUB\n\nUI 100% funcional\nArrastável\nÍcone abrir/fechar\nSwitch individual\n\nScript educacional."
end

------------------------------------------------
-- BOTÕES LATERAIS
------------------------------------------------
local function sideBtn(text, y, callback)
	local b = Instance.new("TextButton", sidebar)
	b.Size = UDim2.new(1,-10,0,30)
	b.Position = UDim2.new(0,5,0,y)
	b.Text = text
	b.TextScaled = true
	b.BackgroundColor3 = Color3.fromRGB(40,40,40)
	b.TextColor3 = Color3.new(1,1,1)
	b.MouseButton1Click:Connect(callback)
end

sideBtn("ESP", 10, espMenu)
sideBtn("Função", 45, funcMenu)
sideBtn("Info", 80, infoMenu)
